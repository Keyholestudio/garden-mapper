# Daily Log — 2026-06-18 (Session 3)

## Summary
Pack 1 (Leafy Veg) committed. Pack 2 (Root Veg) generated, iterated, committed. Pipeline v9 broken — reverted and rewrote as v10.

---

## What We Did

### 1. Pack 1 — Leafy Veg committed
- 9 stickers copied to `app/public/stickers/` + `stickers/`
- `pack-vegetables-leafy.js` populated with all 9 entries
- Rows moved from `PLANT-STAGING.md` → `PLANT-DATABASE.md` with Sticker IDs
- Validator updated to auto-scan all pack files (was hardcoded to PACK1/2/3 only)
- Validator regex updated to handle `key: 'value'` (with space) not just `key:'value'`
- Committed: `7e12dc7`

### 2. Pack 2 — Root Veg generated
- 9 plants: Celeriac, Jerusalem Artichoke, Salsify, Scorzonera, Hamburg Parsley, Sweet Potato, Oca, Skirret, Yacon
- Multiple rounds of prompt iteration (Hamburg Parsley, Sweet Potato, Salsify)
- Key prompt lessons: "Hidden root", "Nothing showing beneath the soil line", "No tubers visible below the soil"

### 3. Pipeline v9 broke — rewrote as v10
- v9 had: second-pass whole-image spill suppression + white border detection loop → grey halos, incomplete BG removal, some images not processed at all
- v10: reverted to same simple structure as original working green pipeline (v4), ported to magenta
  - Tolerance=80, soft_range=40 (same as green)
  - Edge-only spill (semi-transparent pixels only)
  - 20% corner watermark erase
  - No border detection, no multi-pass
- All 9 root veg raws reprocessed through v10
- All 9 leafy veg raws also reprocessed through v10 and committed

### 4. Pack 2 committed
- `pack-vegetables-root.js` populated with all 9 entries
- `PLANT-DATABASE.md` updated with Root Veg section
- `PLANT-STAGING.md` root veg section removed
- Committed: `3ce76b2`

### 5. Docs updated
- `LESSONS.md`: L034 simplified, L035 added (pipeline simplicity rule)
- `STICKER-PROMPT-GUIDE.md` Section 7b: updated to v10 settings + "no second-pass" rule
- Root veg shape prompts saved to `PLANT_LOOKUP` in `sticker-generate-one.py`

---

## Key Lessons
- Pipeline: keep it simple. Edge-only spill + chroma key distance is sufficient. Multi-pass = grey halos.
- Root veg prompts: "Hidden root" and "Nothing showing beneath soil line" are the magic words.
- Salsify: explicitly say "NOT a dandelion" — same family (Asteraceae), Gemini confuses them.
- Validator: must scan ALL pack files dynamically, not hardcoded list.

## State at End
- Packs done: 1 (Leafy Veg) ✅, 2 (Root Veg) ✅
- Next: Pack 3 (Bulb Veg) — 7 plants in PLANT-STAGING.md
- Git: `3ce76b2` on main
